/**
 * Displays a message that prints in STDOUT.
 * @param {String} message The message to display.
 * @author Daniel Mekonnen <https://github.com/DannyGetch>
 */
const displayMessage = (message) => {
  console.log(message);
};

module.exports = displayMessage;
